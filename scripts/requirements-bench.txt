# Pixel dependencies must match the independently qualified reader runtime.
numpy==2.5.1
Pillow==12.0.0
pyserial==3.5
# OpenCV uses a four-part package version.
opencv-python==4.14.0.94
stbt-core==34.12.0
