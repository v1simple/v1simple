{
  "budgets": {
    "ci-test": {
      "description": "Local and GitHub CI gate (ci-test.sh), calibrated for cold GitHub runners",
      "max_seconds": 1800
    },
    "production-artifacts": {
      "description": "Shared clean frontend, firmware, and LittleFS artifact build",
      "max_seconds": 600
    }
  }
}
