name: Release

# Manual release workflow. Keep this off push-to-main so creating or updating
# the public repository cannot publish a release accidentally.
#
# Bump FIRMWARE_VERSION in include/config.h before running.
#
# Steps:
#   1. Run ci-test.sh
#   2. Read version from include/config.h
#   3. Guard against conflicting tag collision
#   4. Build firmware + filesystem using web assets deployed by ci-test.sh
#   5. Merge binaries for ESP Web Tools
#   6. Stage the GitHub Pages web installer
#   7. Create or reuse git tag for this commit
#   8. Create GitHub Release
#   9. Deploy the web installer to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: interface/package-lock.json

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Cache PlatformIO
        uses: actions/cache@v5
        with:
          path: |
            ~/.platformio/packages
            ~/.platformio/platforms
            ~/.platformio/.cache
          key: ${{ runner.os }}-pio-${{ hashFiles('platformio.ini') }}
          restore-keys: ${{ runner.os }}-pio-

      - name: Install toolchain
        run: |
          pip install "platformio>=6.1.19,<7" esptool
          cd interface && npm ci

      - name: Run ci-test.sh gate
        run: ./scripts/ci-test.sh

      - name: Read version from config.h
        id: version
        run: |
          VERSION=$(grep '#define FIRMWARE_VERSION' include/config.h | sed 's/.*"\(.*\)".*/\1/')
          if [ -z "$VERSION" ]; then
            echo "::error::Could not parse FIRMWARE_VERSION from include/config.h"
            exit 1
          fi
          echo "Version: $VERSION"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=v$VERSION" >> $GITHUB_OUTPUT

      - name: Guard against tag collision
        env:
          RELEASE_TAG: ${{ steps.version.outputs.tag }}
        run: |
          git fetch --tags --force origin
          if git rev-parse "$RELEASE_TAG" >/dev/null 2>&1; then
            EXISTING_SHA="$(git rev-list -n 1 "$RELEASE_TAG")"
            if [ "$EXISTING_SHA" = "$GITHUB_SHA" ]; then
              echo "Tag $RELEASE_TAG already points at this commit; continuing release rerun."
            else
              echo "::error::Tag $RELEASE_TAG already exists at $EXISTING_SHA. Bump FIRMWARE_VERSION in include/config.h before releasing."
              exit 1
            fi
          fi

      - name: Build firmware
        run: pio run -e waveshare-349 -j 1

      - name: Build filesystem
        run: pio run -e waveshare-349 -t buildfs -j 1

      - name: Check LittleFS image/runtime compatibility
        run: python3 scripts/check_littlefs_image_compatibility.py --candidate .pio/build/waveshare-349/littlefs.bin

      - name: Merge firmware for ESP Web Tools
        env:
          RELEASE_TAG: ${{ steps.version.outputs.tag }}
        run: |
          mkdir -p release
          cp .pio/build/waveshare-349/bootloader.bin release/
          cp .pio/build/waveshare-349/partitions.bin release/
          cp .pio/build/waveshare-349/firmware.bin release/
          cp .pio/build/waveshare-349/littlefs.bin release/

          esptool --chip esp32s3 merge-bin \
            -o release/merged-firmware.bin \
            --flash-mode dio \
            --flash-freq 80m \
            --flash-size 16MB \
            0x0000 release/bootloader.bin \
            0x8000 release/partitions.bin \
            0x20000 release/firmware.bin \
            0xda0000 release/littlefs.bin

          python3 scripts/check_release_image_info.py \
            --image release/merged-firmware.bin \
            --env waveshare-349

          python3 - <<'PY'
          import json, os
          from pathlib import Path
          manifest = {
              "name": "V1-Simple",
              "version": os.environ["RELEASE_TAG"],
              "home_assistant_domain": "",
              "funding_url": "",
              "new_install_prompt_erase": True,
              "new_install_improv_wait_time": 0,
              "builds": [{
                  "chipFamily": "ESP32-S3",
                  "parts": [{"path": "merged-firmware.bin", "offset": 0}],
              }],
          }
          Path("release/manifest.json").write_text(json.dumps(manifest, indent=2) + "\n")
          PY

      - name: Stage GitHub Pages installer
        run: |
          mkdir -p release/pages
          cp web-installer/index.html release/pages/index.html
          cp release/manifest.json release/pages/manifest.json
          cp release/merged-firmware.bin release/pages/merged-firmware.bin
          python3 scripts/check_web_installer_page.py --site-dir release/pages

      - name: Upload image-info evidence
        uses: actions/upload-artifact@v6
        with:
          name: release-image-info-${{ steps.version.outputs.tag }}
          path: release/merged-firmware-image-info.txt
          if-no-files-found: error

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload GitHub Pages installer artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: release/pages

      - name: Create and push tag
        env:
          RELEASE_TAG: ${{ steps.version.outputs.tag }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git rev-parse "$RELEASE_TAG" >/dev/null 2>&1; then
            echo "Tag $RELEASE_TAG already exists at this commit; reusing it."
          else
            git tag "$RELEASE_TAG"
            git push origin "$RELEASE_TAG"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: ${{ steps.version.outputs.tag }}
          body_path: RELEASE_NOTES.md
          generate_release_notes: true
          files: |
            release/*.bin
            release/manifest.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Summary
        run: |
          {
            echo "## Released ${{ steps.version.outputs.tag }}"
            echo ""
            echo "- Tag created or reused for this commit"
            echo "- GitHub Release published with firmware, filesystem, merged installer binary, and ESP Web Tools manifest"
            echo "- Image-info report uploaded as workflow evidence"
            echo "- GitHub Pages installer deployed from the same manifest and merged image"
          } >> $GITHUB_STEP_SUMMARY

  deploy-pages:
    needs: release
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy GitHub Pages installer
        id: deployment
        uses: actions/deploy-pages@v4
